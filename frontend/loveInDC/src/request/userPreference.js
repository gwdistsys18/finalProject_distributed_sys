import axios from '../../config/http';

const url = "/userpreference";

export const getUserInfoById = () => {
  axios.get()
}

export const updateUserPreferenceById = () => {
  axios.put(url + "/preference", {
    
  })
}
